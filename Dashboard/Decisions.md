---
doc: dashboard
updated: 2026-09-07
status: active
---

# Decisions

_Last regenerated by `brain-pipeline` on 2026-08-28._

## Open

- Canonical engine name for Manufacturing's 4-pillar architecture (`Fechon` vs `Husk` vs `Allternit Engineering`).
- Whether to use Playwright or direct API providers for marketing asset generation.

## Resolved

- Browser-based MuJoCo viewer for Husks simulator (`@mujoco/mujoco` + Three.js) deployed at `try.allternit.com`.

- **Provider Routing design (2026-09-07, product doc: `Products/ProviderRouting.md`):**
  - Execution mode: **passthrough-first** (OpenRouter-style aggregator, `provider` object forwarded per request); native mode (AllternitOS Fabric Runtime workers in the backend catalog) later, same policy shape.
  - v1 scope: **Allternit-operated routing only**; BYO-subscription routing (requests billed to the customer's own cloud console sub) is a fast-follow, not v1.
  - Override semantics: per-model overrides follow the **currently-active resolved model** (matching Hermes) — including after A:// tier substitution; switches, fallbacks, cron jobs, and subagents each get their own pins.

- Folder naming stays Title Case; Compute/Manufacturing/Robotics/Spaces division boundaries locked in `Divisions/CONVERGENCE.md`.
- Compute is design authority; Manufacturing is production authority; Robotics is autonomy authority; Spaces is environment authority.
- Brain is the workspace layer (Obsidian model), not just a docs repo.
